import { NgxGovukBackLinkDirective } from './back-link.directive';

describe('BackLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxGovukBackLinkDirective();
    expect(directive).toBeTruthy();
  });
});
