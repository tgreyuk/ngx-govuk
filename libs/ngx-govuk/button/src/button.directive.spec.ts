import { NgxGovukButtonDirective } from './button.directive';

describe('ButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxGovukButtonDirective();
    expect(directive).toBeTruthy();
  });
});
