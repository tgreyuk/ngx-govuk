import { NgxGovukTextareaDirective } from './textarea.directive';

describe('NgxGovukTextareaDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxGovukTextareaDirective();
    expect(directive).toBeTruthy();
  });
});
